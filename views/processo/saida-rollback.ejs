<%- include('../components/breadcrumb', { link: '/historico/saidas', paginaAnterior: 'Saídas'}) %>
<%- include('../components/popup/pinModal') %>

<div class="flex flex-col gap-4 mt-5">
    <div class="flex flex-col items-center gap-1 p-8 bg-white rounded-xl border border-gray-200">
        <img class="h-24 w-24" src="/images/icones/desfazer.png" alt="Desfazer">
        <h1 class="text-2xl font-semibold text-green-800">Desfazer Saída</h1>
        <p class="text-gray-500 text-center">Ao desfazer esta saída, a quantidade será devolvida ao estoque.<br> O CMV será recalculado instantaneamente.</p>
    </div>

    <div class="flex flex-col gap-1 bg-white rounded-xl border border-gray-200 p-6">
        <select id="motivo" class="border border-gray-400 p-1 appearance-none rounded-md outline-none" id="motivo" name="motivo_cancelamento">
        <option value="">Selecione o motivo</option>
        <option value="Erro lancamento">Erro no lançamento</option>
        <option value="Produto incorreto">Produto incorreto</option>
        <option value="Quantidade incorreta">Quantidade informada incorreta</option>
        <option value="Saída duplicada">Saída duplicada</option>
        <option value="Criacao por engano">Saída criada por engano</option>
        </select>
        <button id="desfazer" class="mt-8 bg-green-600 hover:bg-green-700 transition-colors duration-200 text-white px-4 py-2 rounded-full cursor-pointer">Desfazer Saída</button>
    </div>
    <p class="text-xs text-gray-600 text-right">
        ID da movimentação: <%= saidaId %>
    </p>
</div>

<script>
    const pinModal = document.getElementById('pin-modal');
    const confirmar = document.getElementById('confirmar-pin');
    const pinInput = document.getElementById('pin-input');
    const desfazer = document.getElementById('desfazer');
    
    desfazer.addEventListener('click', () => {
        pinModal.classList.remove('hidden');
        pinInput.focus();
    });

    desfazer.addEventListener('click', () => {
        if (!motivo.value) {
            pinModal.classList.add('hidden');
            alert('Selecione um motivo para prosseguir');
        }
    });

    confirmar.addEventListener('click', () => {
        if (pinInput.value !== '<%= userPin %>') {
            alert('Pin incorreto');
            return;
        }

        const motivo = document.getElementById('motivo').value;
        if (motivo) {
            fetch('https://movimentacoes.taiksu.com.br/api/desfazer/saida', {
                method: 'PUT',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({
                    motivo,
                    responsavel_id: '<%= user_id %>',
                    id: '<%= saidaId %>'
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert(data.message);
                    window.location.href = 'https://estoque.taiksu.com.br/historico/saidas';
                }
            })
            .catch(error => console.error('Error:', error));
        }
    });
</script>
